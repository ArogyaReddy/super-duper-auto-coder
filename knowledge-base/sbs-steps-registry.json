{
  "metadata": {
    "generated_date": "2025-08-06",
    "purpose": "SBS_Automation step registry to prevent ambiguous steps in auto-coder",
    "version": "1.0"
  },
  "existing_navigation_steps": [
    {
      "step": "When('Alex navigates to Billing Home page')",
      "file": "SBS_Automation/steps/runMod/billing/billing-home-steps.js",
      "purpose": "Navigate to billing landing page",
      "reusable": true
    },
    {
      "step": "When('Alex navigates to State Tax page')",
      "file": "SBS_Automation/steps/runMod/company/adding-state-tax-steps.js",
      "purpose": "Navigate to state tax configuration",
      "reusable": true
    }
  ],
  "existing_verification_steps": [
    {
      "step": "Then('Billing Homepage should be displayed')",
      "file": "SBS_Automation/steps/runMod/billing/billing-home-steps.js",
      "purpose": "Verify billing page loaded",
      "reusable": true
    },
    {
      "step": "Then('Billing & Invoices page is displayed')",
      "file": "SBS_Automation/steps/runMod/billing/billing-home-steps.js",
      "purpose": "Verify billing invoices page displayed",
      "reusable": true
    }
  ],
  "ambiguous_patterns_to_avoid": [
    {
      "pattern": "Alex navigates to {string} page",
      "conflict_count": 22,
      "reason": "Too generic, conflicts with multiple existing steps",
      "alternative": "Use specific page names like 'Alex navigates to Billing Home page'"
    },
    {
      "pattern": "Alex clicks {string} button",
      "conflict_count": 15,
      "reason": "Generic button clicking conflicts with parameterized steps",
      "alternative": "Use context-specific steps like 'Alex clicks the billing invoices Get Started button'"
    },
    {
      "pattern": "Alex verifies {string} page title is displayed",
      "conflict_count": 8,
      "reason": "Generic page title verification",
      "alternative": "Use domain-specific verification steps"
    }
  ],
  "safe_step_patterns": [
    {
      "pattern": "Alex verifies the {domain} {element} exists and is clickable",
      "example": "Alex verifies the billing invoices Get Started button exists and is clickable",
      "benefit": "Domain-specific, unlikely to conflict"
    },
    {
      "pattern": "Alex clicks the {domain} {element}",
      "example": "Alex clicks the billing invoices Learn More link", 
      "benefit": "Context-specific, no ambiguity"
    },
    {
      "pattern": "Alex verifies all {domain} page elements are properly loaded",
      "example": "Alex verifies all billing invoices page elements are properly loaded",
      "benefit": "Domain-scoped verification"
    }
  ],
  "reuse_priority_order": [
    "1. Search existing SBS steps first",
    "2. Reuse exact matches where possible",
    "3. Create domain-specific steps if needed",
    "4. Validate no conflicts before generation"
  ]
}
